# Linting Policy
Purpose: Define lint rules enforced in CI.

## Requirements
- Linting must run in CI on every push and PR.
- Fail the pipeline on lint violations.

## Scope
- Python files in `services/`, `skills/`, `tests/`, and root modules.

## Tools (placeholder)
- ruff or flake8 (choose one when implementation begins).
