# Testing Policy
Purpose: Define test types, coverage expectations, and mapping to specs.

## Requirements
- Tests must reference FR/NFR IDs.
- Tests should fail when contracts are missing.
- CI runs tests via `make test`.
